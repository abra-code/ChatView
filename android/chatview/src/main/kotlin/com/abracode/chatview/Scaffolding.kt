package com.abracode.chatview

// Stage A2 scaffolding placeholder. It only anchors the main source set so the module compiles and is
// consumable as `com.abracode:chatview` before any ported source exists (the demo references it so the composite
// dependency is a compile-time fact, not just configuration). A3 begins replacing this with the real port
// (ChatModel.kt, ChatConfiguration.kt, ...); delete this file once a real public type lives here.
const val CHATVIEW_MODULE_SCAFFOLD = "com.abracode:chatview:0.1.0"
